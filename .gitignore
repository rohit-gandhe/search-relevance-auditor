# --- credentials: never committed ---
.env
.env.local
secrets/*
!secrets/.gitkeep

# --- personal rebuild notes: carries the AWS account id and the sheet id ---
.env.template

# --- build output ---
build/
.gradle/
*.class

# --- runtime state: regenerated by every run, reset by scripts/snapshot.sh ---
data/cache/
data/index/
data/processed.txt
data/proposals/
data/run-log.jsonl
*.pid
logs/

# --- raw WANDS: 92 MB, and scripts/fetch-wands.sh downloads it in 2 minutes.
#     The curated 617-product slice IS committed; the raw corpus is not. ---
data/product.csv
data/label.csv
data/query.csv

.DS_Store
.idea/
